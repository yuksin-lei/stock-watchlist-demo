services:
  frontend:
    build: .
    container_name: watchlist-frontend
    restart: unless-stopped
    ports:
      - "80:80"
    environment:
      # 后端地址，通过环境变量注入 nginx 配置
      BACKEND_URL: http://host.containers.internal:8080
